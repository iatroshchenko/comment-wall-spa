pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("Dev environment -- Prepare") {
            steps {
                echo "Jenkins test"
            }
        }
    }
}
